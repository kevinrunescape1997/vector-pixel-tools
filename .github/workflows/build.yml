name: Build executables

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
    inputs:
      include_tkinterdnd2:
        description: "Include tkinterdnd2 (drag & drop) in the build"
        type: boolean
        default: false

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest, ubuntu-20.04]

    runs-on: ${{ matrix.os }}

    env:
      PYTHON_VERSION: "3.11"
      APP_NAME: "svg_pixel_rect_optimizer_gui"
      INCLUDE_DND: ${{ github.event_name == 'workflow_dispatch' && inputs.include_tkinterdnd2 || 'false' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Upgrade pip
        run: python -m pip install --upgrade pip

      - name: Install build dependencies
        run: python -m pip install pyinstaller

      - name: Install tkinterdnd2 (optional)
        if: env.INCLUDE_DND == 'true'
        run: python -m pip install tkinterdnd2

      # ---------------- Windows ----------------
      - name: Build (Windows) -> zip exe
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $args = @("--noconfirm","--clean","--windowed","--onefile","--name",$env:APP_NAME,"svg_optimizer_gui.py")
          if ($env:INCLUDE_DND -eq "true") { $args += @("--collect-data","tkinterdnd2") }
          pyinstaller @args
          Compress-Archive -Path "dist\$env:APP_NAME.exe" -DestinationPath "$env:APP_NAME-Windows.zip" -Force

      - name: Upload artifact (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.APP_NAME }}-Windows
          path: ${{ env.APP_NAME }}-Windows.zip
          retention-days: 30

      # ---------------- macOS ----------------
      - name: Build (macOS) -> zip .app
        if: runner.os == 'macOS'
        shell: bash
        run: |
          set -euxo pipefail
          ARGS=(--noconfirm --clean --windowed --name "$APP_NAME" svg_optimizer_gui.py)
          if [[ "$INCLUDE_DND" == "true" ]]; then ARGS+=(--collect-data tkinterdnd2); fi
          pyinstaller "${ARGS[@]}"
          zip -r "${APP_NAME}-macOS.zip" "dist/${APP_NAME}.app"

      - name: Upload artifact (macOS)
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.APP_NAME }}-macOS
          path: ${{ env.APP_NAME }}-macOS.zip
          retention-days: 30

      # ---------------- Linux (ubuntu-20.04) ----------------
      - name: Build (Linux) -> AppImage
        if: runner.os == 'Linux'
        shell: bash
        run: |
          set -euxo pipefail

          # Build a onefile binary on ubuntu-20.04 (better glibc compatibility)
          ARGS=(--noconfirm --clean --windowed --onefile --name "$APP_NAME" svg_optimizer_gui.py)
          if [[ "$INCLUDE_DND" == "true" ]]; then ARGS+=(--collect-data tkinterdnd2); fi
          pyinstaller "${ARGS[@]}"
          chmod +x "dist/${APP_NAME}"

          # Create AppDir structure
          APPDIR="${APP_NAME}.AppDir"
          rm -rf "$APPDIR"
          mkdir -p "$APPDIR/usr/bin" \
                   "$APPDIR/usr/share/applications" \
                   "$APPDIR/usr/share/icons/hicolor/256x256/apps"

          # Copy binary
          cp "dist/${APP_NAME}" "$APPDIR/usr/bin/${APP_NAME}"

          # Generate a simple icon (so the .desktop file has something valid)
          python - <<'PY'
          from PIL import Image, ImageDraw
          img = Image.new("RGBA", (256, 256), (32, 32, 32, 255))
          d = ImageDraw.Draw(img)
          d.rectangle([24, 24, 232, 232], outline=(200, 200, 200, 255), width=6)
          d.text((40, 110), "SVG", fill=(240, 240, 240, 255))
          img.save("icon.png")
          PY
          # Pillow is not guaranteed preinstalled; install it on demand.
        continue-on-error: false

      - name: Install Pillow (Linux) for icon generation
        if: runner.os == 'Linux'
        shell: bash
        run: |
          set -euxo pipefail
          python -m pip install pillow

      - name: Finalize AppImage (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          set -euxo pipefail

          APPDIR="${APP_NAME}.AppDir"

          # Generate icon (now that pillow is installed)
          python - <<'PY'
          from PIL import Image, ImageDraw
          img = Image.new("RGBA", (256, 256), (32, 32, 32, 255))
          d = ImageDraw.Draw(img)
          d.rectangle([24, 24, 232, 232], outline=(200, 200, 200, 255), width=6)
          d.text((40, 110), "SVG", fill=(240, 240, 240, 255))
          img.save("icon.png")
          PY

          cp icon.png "$APPDIR/usr/share/icons/hicolor/256x256/apps/${APP_NAME}.png"

          # .desktop launcher (embedded in AppImage)
          cat > "$APPDIR/usr/share/applications/${APP_NAME}.desktop" <<EOF
          [Desktop Entry]
          Type=Application
          Name=SVG Pixel-Rect Optimizer
          Exec=${APP_NAME}
          Icon=${APP_NAME}
          Categories=Utility;Graphics;
          Terminal=false
          EOF

          # AppRun: point to the binary
          ln -sf "usr/bin/${APP_NAME}" "$APPDIR/AppRun"

          # Download appimagetool and build an AppImage
          curl -L -o appimagetool.AppImage \
            https://github.com/AppImage/AppImageKit/releases/download/continuous/appimagetool-x86_64.AppImage
          chmod +x appimagetool.AppImage

          # Avoid needing FUSE by extracting the tool first
          ./appimagetool.AppImage --appimage-extract >/dev/null
          ./squashfs-root/AppRun "$APPDIR" "${APP_NAME}-Linux-x86_64.AppImage"

      - name: Upload artifact (Linux AppImage)
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.APP_NAME }}-Linux-AppImage
          path: ${{ env.APP_NAME }}-Linux-x86_64.AppImage
          retention-days: 30

